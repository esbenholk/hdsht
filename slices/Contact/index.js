import React from "react";

import ContactSection from "../../components/ContactSection/ContactSection";

const Contact = ({ slice }) => (
  <section>
    <ContactSection slice={slice} />
  </section>
);

export default Contact;
